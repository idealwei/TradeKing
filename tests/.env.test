# Test environment variables
# These are automatically loaded during testing

# Model Configuration (for testing)
TRADE_AGENT_MODEL=gpt5
TRADE_AGENT_TEMPERATURE=0.4
TRADE_AGENT_MAX_OUTPUT_TOKENS=1024

# API Keys (mock values for testing)
OPENAI_API_KEY=test-openai-key-12345
DEEPSEEK_API_KEY=test-deepseek-key-12345

# Longbridge Configuration (mock values)
LONGBRIDGE_ACCESS_TOKEN=test-longbridge-token
LONGBRIDGE_BASE_URL=https://mock-api.longbridge.test
LONGBRIDGE_TIMEOUT=10.0

# Database Configuration (in-memory for tests)
DATABASE_URL=sqlite:///:memory:

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8000
DEBUG=true

# Scheduler Configuration (disabled for tests)
SCHEDULER_ENABLED=false
SCHEDULER_INTERVAL_MINUTES=5

# Logging
LOG_LEVEL=DEBUG
LOG_FILE=/tmp/tradeking_test.log
